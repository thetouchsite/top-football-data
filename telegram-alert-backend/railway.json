{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "watchPatterns": ["telegram-alert-backend/**"]
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "restartPolicyType": "ALWAYS"
  }
}
