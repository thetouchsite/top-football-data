TOP FOOTBALL DATA
TODO SVILUPPO OPERATIVO

Data aggiornamento: 2026-04-17
Stato documento: allineato al codice (inclusi UI responsive, probabilita O/U-GG, value bet, toast)

====================================================================
LINEE GUIDA (COSA E' IL PROGETTO ORA)
====================================================================

- Feed calcio: un solo provider dati, Sportmonks Football API v3.
- Sportradar: rimosso dal repository (nessun pacchetto, nessun fallback, nessun client).
- OddsMatrix: mai integrato come prodotto; lo stub e i riferimenti in health/catalog sono stati
  rimossi; quote e comparatore restano da costruire sopra Sportmonks (e UI interna).
- ID canonico match: fixture id Sportmonks (`/match/[id]`, API fixtures, watchlist/search).
- Health (`GET /api/health`): readiness su MongoDB, Stripe e `providers.sportmonks` soltanto.
- `GET /api/football/odds/futures`: ancora placeholder fino a integrazione outrights Sportmonks.
- Dati Live (pagina `/dati-live`, nav, fetch livescore su altre schermate): controllati da
  `NEXT_PUBLIC_FEATURE_DATI_LIVE=true` (vedi `src/lib/feature-flags.js` e README). Default spento.

====================================================================
ARCHITETTURA RILEVANTE (PUNTATORI)
====================================================================

- Catalogo competizioni: `src/lib/competitions/catalog.js`
- Domain: `src/lib/domain/*`
- Provider Sportmonks: `src/lib/providers/sportmonks/index.js`
- Orchestrazione feed: `src/server/football/service.js`
- Priorita leghe / ranking: `src/lib/sportmonks-priority-league-ids.js`, ordinamento in service
- Env opzionali feed: `SPORTMONKS_SCHEDULE_LEAGUE_FILTER_STRICT`, `SPORTMONKS_SCHEDULE_LEAGUE_IDS`, ecc.
- Shell/UI: `src/app/layout.jsx`, `src/components/layout/*`, schermate in `src/screens/*`
- Probabilita O/U e GG (schedule/match): calcolo in `src/lib/providers/sportmonks/index.js`
  — priorita (1) predizioni API yes/no se presenti, (2) percentuali modello derivato `buildGoalMarkets`
  (`ouProbDerived` / `ggProbDerived`, stesse formule delle quote derivate), (3) implicite da quote
  bookmaker (`100/quota`), (4) fallback implicite sulle quote mostrate. Campi payload: `ouProb`, `ggProb`.
- Value bet (UI): `src/components/shared/ValueBetBadge.jsx`; dato mostrato da `buildDerivedValueBet` in
  `sportmonks/index.js` (euristica interna) — non e l'API ufficiale Sportmonks "Value bets".
- Value bet Sportmonks ufficiale: add-on Predictions; endpoint `predictions/value-bets` e
  `predictions/value-bets/fixtures/{id}` (fair_odd, odd, bookmaker, is_value, stake). Vedi
  https://docs.sportmonks.com/v3/tutorials-and-guides/tutorials/odds-and-predictions/predictions/value-bet
  — integrazione futura quando il piano cliente lo consente.
- Toaster: `src/components/ui/toast.jsx` usa `pointer-events-none` su viewport/provider per non
  bloccare click su navbar/header (`z` toast sopra la barra).
- Live odds UI (griglia da props, non brand esterno): `src/components/live/LiveOddsGrid.jsx`
- Auth/account: `src/app/api/auth`, `src/lib/auth-*.js`, API `/api/account/*`
- Documentazione operativa sintetica: `README.md`
- Feature flag: `src/lib/feature-flags.js`

====================================================================
CRONOLOGIA SINTETICA (MIGRAZIONI GIA' FATTE)
====================================================================

Fase iniziale — hardening e real-feed-first
- Health, billing, domain layer; mock tolti dai percorsi core; provenance/freshness in UI.

Migrazione Sportmonks-first (core)
- Schedule, fixture, live, statistiche, lineups, standings, coaches/referees/formations, squad,
  pre-match odds (in base al piano); incremental live (`inplay` + `latest` dove previsto).
- Canonical ids Sportmonks; dashboard/modelli/dati live/match detail/search/watchlist riallineati.

Pass UX / shell
- Ranking per priority league ids; calendario globale di default con filtro leghe opzionale;
  landing, premium, navbar, shell aggiornati.

Auth e account
- Sessione server-side, ruoli, Google opzionale, fallback SQLite auth in dev se Mongo irraggiungibile.
- Persistenza DB: profilo, preferenze, watchlist, following; pagine `/preferiti`, `/seguiti`;
  `/watchlist` redirect legacy.

Aprile 2026 — pulizia provider
- Rimozione completa dipendenze e codice Sportradar (inclusi artefatti `.api` collegati).
- Rimozione stub OddsMatrix; health/catalog solo Sportmonks; UI `LiveOddsGrid` + copy MultiBet/doc
  allineati a "solo Sportmonks" per quote/comparatore.

Aprile 2026 — roadmap step 1 (feature flag Dati Live)
- Variabile `NEXT_PUBLIC_FEATURE_DATI_LIVE` (solo `true` abilita): `src/lib/feature-flags.js`.
- `src/middleware.js`: con flag spento redirect `/dati-live` -> `/dashboard`.
- Navbar, Dashboard, Modelli predittivi, Landing: niente `getLivescoresInplay` se flag spento; meno
  hit a `GET /api/football/livescores/inplay`. Default senza env = spento (ok anche senza variabile su Vercel).
- Riattivazione prodotto: env `true` + redeploy su Vercel (vedi README).

Aprile 2026 — probabilita O/U-GG, value bet, UX shell
- Normalizer Sportmonks: `extractPredictionBundle` espone `ouProb`/`ggProb` da yes/no API; `buildGoalMarkets`
  espone anche `ouProbDerived`/`ggProbDerived`; `resolveScheduleOuGgProbabilities` unifica API / derivato /
  implicite bookmaker. Match card e match detail mostrano % quando disponibili (`MatchCard.jsx`,
  `MatchDetail.jsx` sezione “Probabilita e quote”).
- Badge value bet: stile chip sobrio, `aria-label`/`title` esplicativi.
- Toast layer: fix click header intercettati da viewport toast (`pointer-events-none` + toast `pointer-events-auto`).
- Layout globale: `overflow-x` / `max-w-full` su shell e body; Landing/login responsive; dropdown nav `100dvw`.

====================================================================
STATO ATTUALE (VERITA' OPERATIVA)
====================================================================

- Build e lint: attesi verdi su main dopo le ultime modifiche.
- Dati Live: visibilita e fetch livescore governati dal flag (default off); su Vercel la variabile
  e opzionale finche la funzione resta spenta.
- Core calcio: Sportmonks reale; nessun secondo provider football nel repo.
- Odds: pre-match Sportmonks integrate dove il piano e gli include lo consentono; live odds
  bookmaker multipli e comparatore "completo" restano da completare sullo stesso feed.
- Value bet UI: `buildDerivedValueBet` (euristica prob/xG); edge Over 2.5 spesso al tetto +10% (commento
  in `index.js`). Per allineamento a "value" Sportmonks vero: integrare endpoint value-bets (add-on
  Predictions) e mappare badge/filtri su quella risposta; finche assente, resta solo il segnale derivato.
- Premium: Stripe checkout/portal presenti; gating e persistenza utente vanno consolidati
  end-to-end in produzione (Mongo stabile, webhook, entitlement).
- MongoDB: in locale spesso TLS/SSL in health; impatta premium e dati non-auth se Atlas non e ok;
  auth puo degradare su SQLite solo in dev.
- Piano Sportmonks: da confermare su add-on (`predictions.type`, `xGFixture`, `inplayOdds`,
  `lineups.xGLineup`, `statistics.type`) rispetto al contratto reale.

====================================================================
GAP APERTI (SINTESI)
====================================================================

1. Odds e comparatore — espandere uso quote Sportmonks in UI (match-by-match, live, best odds,
   movement) in base agli include del piano; niente integrazione provider odds terzi nel repo.
2. Value bet — opzione A: integrare API Sportmonks `predictions/value-bets` (add-on Predictions) e UI;
   opzione B: mantenere solo `buildDerivedValueBet` ma copy chiaro "modello interno". Decidere con cliente.
3. Premium — entitlement server-side, Mongo produzione, webhook Stripe validati.
4. Area utenti — middleware/guard coerenti, OAuth Google stabile in deploy, admin evoluto.
5. Alerting — following persistito ma nessuna delivery reale (email/telegram/in-app).
6. Multi-bet — futures API placeholder; engine reale su mercati veri ancora da fare.
7. Validazione piano — tabella feature -> endpoint Sportmonks -> add-on necessari.

====================================================================
INPUT NECESSARI DAL TEAM
====================================================================

- Piano Sportmonks effettivo (trial/base/premium): quali include sono attivi.
- Conferma su: `predictions.type`, `xGFixture`, `inplayOdds`, `lineups.xGLineup`, `statistics.type`.
- Se si vuole il **value bet come Sportmonks**: add-on **Predictions** attivo e integrazione endpoint
  `predictions/value-bets` (vedi doc ufficiale SM); oggi il prodotto usa solo `buildDerivedValueBet`.
- Stringa MongoDB valida per staging/produzione; strategia TLS/certificati.
- Webhook secret Stripe e price id definitivi.
- Scope V1 free vs premium e messaggistica prodotto.
- Credenziali Google OAuth produzione (`GOOGLE_*`, `NEXT_PUBLIC_GOOGLE_CLIENT_ID`).

====================================================================
DEFINITION OF DONE — "CORE SPORTMONKS" (RIFERIMENTO)
====================================================================

- Preview con match reali Sportmonks su dashboard/modelli/dati live.
- Fixture detail con dati agganciati (standings, coaches, squads, pre-match odds se in piano).
- `/api/health` con `sportmonks` ready; `/match/<sportmonksFixtureId>` end-to-end.
- Search/watchlist con fixture id Sportmonks; build e lint verdi.
- Documenti operativi allineati al codice (questo file + README + documento operativo finale).

====================================================================
BACKLOG — PROSSIME COSE (PRIORITA')
====================================================================

P0. Consolidare area utenti e permessi
- Route pubbliche / autenticate / premium / admin; middleware e guard server-side.
- Profilo, privacy, password/reset; stress test Google OAuth in deploy.
- Premium reale: billing + sessione + ruolo + entitlement.

P1. Mappatura API Sportmonks
- Tabella feature -> endpoint/include -> piano richiesto; cosa resta derivato in app.
- Value bets ufficiali: `GET .../predictions/value-bets` e `.../value-bets/fixtures/{id}` (add-on Predictions) —
  non implementati; oggi badge value = `buildDerivedValueBet`.

P1. Watchlist / following E2E
- Persistenza, edge case fuori finestra schedule, coerenza preferiti vs seguiti.

P2. Validazione feed reale
- Checklist include mancanti; decisione `SPORTMONKS_SCHEDULE_LEAGUE_FILTER_STRICT` vs globale.

P2. MongoDB produzione
- Eliminare blocco TLS in dev/prod; tempi di attesa bootstrap accettabili.

P3. Odds e comparatore su Sportmonks
- Bookmaker comparison, best odds, movement — limitato da cio che il piano espone.

P3. Premium server-side definitivo
- Persistenza utente, webhook Stripe, gating coerente.

P4. Analytics piu profonde — se il piano Sportmonks lo consente.

P4. Live e alerting — follow persistito + canali di notifica.

P5. Multi-bet reale — combinazioni su odds veri, niente placeholder sui percorsi premium finali.

====================================================================
ORDINE CONSIGLIATO (DA QUI)
====================================================================

1. Consolidare auth, ruoli e area account su Mongo stabile (o policy chiara dev vs prod).
2. Chiudere checklist premium: Stripe webhook, entitlement, test end-to-end.
3. Verificare watchlist/following su casi reali e fuori finestra schedule.
4. Definire tabella piano Sportmonks vs feature prodotto; decidere add-on.
5. Validare predictions/xG/in-play odds rispetto al contratto attuale.
6. Policy calendario definitiva: globale + priority ranking vs filtro leghe stretto.
7. Investire su odds/comparatore e live odds nel perimetro Sportmonks.
8. Alerting persistito (dopo modello dati notifiche chiaro).
9. Sostituire placeholder futures e costruire multi-bet su mercati reali.
